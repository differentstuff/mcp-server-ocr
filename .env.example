# OCR MCP Server Configuration

# API Keys (only required for enabled backends)
# Get keys from:
# - Mistral: https://console.mistral.ai/api-keys/
# - DeepSeek: https://platform.deepseek.com/api_keys

# Required if mistral is in ENABLED_BACKENDS
MISTRAL_API_KEY=your-mistral-api-key-here

# Optional - only required if deepseek is in ENABLED_BACKENDS
# DEEPSEEK_API_KEY=your-deepseek-api-key-here

# Backend Configuration
# Available backends: marker, deepseek, mistral
# Comma-separated list of enabled backends
# Default: marker (local) + mistral (API fallback)
ENABLED_BACKENDS=marker,mistral

# Default backend to use (must be in ENABLED_BACKENDS)
DEFAULT_BACKEND=marker

# Processing Settings
MAX_FILE_SIZE_MB=50
TIMEOUT_SECONDS=120

# API Settings
API_TIMEOUT=30
API_MAX_RETRIES=3

# Marker Settings
MARKER_BATCH_SIZE=1